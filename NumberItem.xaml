<UserControl x:Class="LXR_2048.NumberItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Name="BackGroundColor"  Background="WhiteSmoke" Margin="3,3,3,3" >    
        <Viewbox Name="Number_ViewBox"  Stretch="Uniform" DataContext="{Binding}" >
            <TextBlock Text="{Binding Number}" Foreground="Black"  Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" Width="Auto" Height="Auto"/>
        </Viewbox>
    </Grid>
</UserControl>
